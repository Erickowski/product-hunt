import Layout from "../components/layouts/Layout";

export default function Home() {
  return (
    <Layout>
      <h1>Inicio</h1>
    </Layout>
  );
}

// validar email

// !/^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i

// // validar URL

// !/^(ftp|http|https):\/\/[^ "]+$/
